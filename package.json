{
  "name": "teamflow-root",
  "version": "1.0.0",
  "description": "Root configuration for TeamFlow Workspace",
  "private": true,
  "scripts": {
    "install:all": "npm install --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently --kill-others-on-fail \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "db:seed": "npm run db:seed --prefix backend",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/src/**/*.{ts,tsx}": [
      "eslint --fix --max-warnings=0 --no-warn-ignored -c frontend/eslint.config.js",
      "prettier --write"
    ],
    "backend/src/**/*.ts": [
      "eslint --fix --max-warnings=0 --no-warn-ignored -c backend/eslint.config.js",
      "prettier --write"
    ],
    "*.json": [
      "prettier --write"
    ],
    "*.md": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^10.5.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.8.4"
  }
}
